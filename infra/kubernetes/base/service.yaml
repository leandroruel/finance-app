apiVersion: v1
kind: Service
metadata:
  name: finance-api
  labels:
    app: finance-api
spec:
  selector:
    app: finance-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
